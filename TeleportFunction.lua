local teleportService = GetService(TeleportService)
local authKey = GetService(UuidGenerator(Raw))
local place = teleportService.FetchId(Fireteam-Boarding)

local function Teleport(teleportService,authKey,place)
{

    teleportService:Invoke(1:place,2:authKey)
    [

        if (parent.Fail..(errorMessage))
        (
            print(errorMessage)
        )
        else
        (
            print(“success”)
        )

    ]

}

Player.Teleporting:InvokeEvent(teleport)